<!-- filepath: /admin-dashboard/admin-dashboard/src/views/admin/products/detail.ejs -->
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chi tiết sản phẩm - TechMarket Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/admin.css" rel="stylesheet">
</head>
<body>
    <%- include('../layouts/admin.ejs') %>
    <div class="container mt-4">
        <h2>Chi tiết sản phẩm</h2>
        <div class="card">
            <div class="card-body">
                <h5 class="card-title"><%= product.name %></h5>
                <p class="card-text">Mô tả: <%= product.description %></p>
                <p class="card-text">Giá: <%= product.price %> VNĐ</p>
                <p class="card-text">Danh mục: <%= product.category.name %></p>
                <p class="card-text">Trạng thái: <%= product.status %></p>
                <a href="/admin/products/edit/<%= product._id %>" class="btn btn-primary">Chỉnh sửa</a>
                <button class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">Xóa</button>
            </div>
        </div>
    </div>

    <%- include('../partials/admin/deleteModal.ejs') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>