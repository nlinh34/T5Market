<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chi tiết đơn hàng - TechMarket Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/admin.css" rel="stylesheet">
</head>
<body>
    <%- include('../../layouts/admin.ejs') %>
    
    <div class="container mt-4">
        <h2>Chi tiết đơn hàng</h2>
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Thông tin đơn hàng</h5>
                <p class="card-text"><strong>ID Đơn hàng:</strong> <%= order.id %></p>
                <p class="card-text"><strong>Khách hàng:</strong> <%= order.customerName %></p>
                <p class="card-text"><strong>Địa chỉ giao hàng:</strong> <%= order.shippingAddress %></p>
                <p class="card-text"><strong>Tổng tiền:</strong> <%= order.totalAmount %> VNĐ</p>
                <p class="card-text"><strong>Trạng thái:</strong> <%= order.status %></p>
                
                <h5 class="mt-4">Sản phẩm trong đơn hàng</h5>
                <ul class="list-group">
                    <% order.products.forEach(product => { %>
                        <li class="list-group-item">
                            <strong><%= product.name %></strong> - <%= product.quantity %> x <%= product.price %> VNĐ
                        </li>
                    <% }) %>
                </ul>
                
                <div class="mt-4">
                    <a href="/admin/orders" class="btn btn-primary">Quay lại danh sách đơn hàng</a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>