<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chi tiết người dùng - TechMarket Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/admin.css" rel="stylesheet">
</head>
<body>
    <%- include('../../layouts/admin.ejs') %>
    <div class="container mt-4">
        <h2>Chi tiết người dùng</h2>
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Thông tin người dùng</h5>
                <p class="card-text"><strong>ID:</strong> <%= user._id %></p>
                <p class="card-text"><strong>Tên:</strong> <%= user.name %></p>
                <p class="card-text"><strong>Email:</strong> <%= user.email %></p>
                <p class="card-text"><strong>Ngày tạo:</strong> <%= user.createdAt.toLocaleDateString() %></p>
                <p class="card-text"><strong>Trạng thái:</strong> <%= user.isActive ? 'Kích hoạt' : 'Không kích hoạt' %></p>
                <a href="/admin/users" class="btn btn-primary">Quay lại danh sách người dùng</a>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>